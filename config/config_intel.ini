# DEVELOPMENT PLACEHOLDER — mirrors IPsetting_sample.csv layout so the
# core pipeline can be exercised on the Ubuntu dev box.
# REPLACE with real output from `tools/inventory.py` on the AlmaLinux machine.

[board]
name = INTEL-DEV-PLACEHOLDER
description = matches sample CSV; not real hardware capture

[port_map]
LAN1 = enp3s0
LAN2 = enp4s0
LAN3 = enp1s0f3
LAN4 = enp1s0f2
LAN5 = enp1s0f1
LAN6 = enp1s0f0
