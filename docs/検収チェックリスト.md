# 検収チェックリスト（受入基準 仕様書16章）

対象機で下記を上から順に実施。各項目の「合格条件」を満たせば検収完了。

## Phase 1 — Intel検証機

### A. 事前準備
- [ ] ツール一式を配置し、`python3 --version` が 3.9 系であることを確認
- [ ] （GUI使用時）日本語フォント: `sudo dnf install -y google-noto-sans-cjk-jp-fonts`
      ※ ビルド版 `dist/ipset-gui` を使う場合 `python3-tkinter` は不要
      （Pythonソースから直接実行する場合のみ `sudo dnf install -y python3-tkinter`）
- [ ] 実インターフェース取得: `python3 tools/inventory.py`
      → 出力の `[port_map]` を物理LAN1〜6に合わせ `config/config_intel.ini` に保存
- [ ] 物理ポート確認（任意）: `sudo ethtool -p <ifname> 10` でLED点滅を確認

### B. 検収項目（仕様書16章）

| # | 項目 | コマンド | 合格条件 |
|---|------|----------|----------|
| 1 | Excel/CSV読込 | `python3 -m ipset.cli 支給ファイル --list` | シリアル一覧が表示される |
| 2 | 期待値表示（変更なし） | `python3 -m ipset.cli 支給ファイル --serial <SN>` | LAN1〜6の設定内容とnmcliコマンドが表示 |
| 3 | 6ポート設定適用 | `sudo python3 -m ipset.cli 支給ファイル --serial <SN> --commit` | 全ポートに適用が実行される |
| 4 | 読み戻し・照合 | （上記に含む） | 実際値が読み戻され期待値と比較される |
| 5 | OK/NG表示 | （上記出力） | `Result: PASS (OK=6 NG=0)` |
| 6 | CSVログ保存 | `cat logs/result.csv` | 日時/SN/LAN/IP/…/結果 が追記されている |
| 7 | 終了コード | `echo $?`（commit直後） | PASS時 `0` |

### B-2. 追加機能の検収（GUI・生産性/出荷対応）

対象機のGUIで実施。

| # | 項目 | 操作 | 合格条件 |
|---|------|------|----------|
| 8 | シリアル絞り込み入力 | 対象シリアル欄にシリアルの一部を入力（例「199」） | 該当シリアルが候補表示され、クリックで選択できる |
| 9 | ログのデスクトップ保存 | 本適用（ドライラン✓を外して設定開始）後、デスクトップを確認 | `result.csv` がデスクトップに保存・追記されている |
| 10 | ×での削除確認（本適用後） | 本適用・ログ出力後にウィンドウ右上の × を押す | 削除確認ダイアログが表示される（「はい/いいえ/キャンセル」） |
| 11 | ドライランのみの終了 | ドライランのみ実行し × を押す | 削除確認は出ず、通常どおり終了する |
| 12 | 自己アンインストール | #10で「はい」→ログ削除は任意選択 | ツールが端末から削除される（`/opt/ipset`・アイコン・sudoers）。ログは選択に従う |

> #12 は端末からツールを実際に削除します。検証機で試す場合は削除後に
> `bash build.sh` → `sudo bash setup_desktop.sh` で再導入してください。

### C. 異常系の確認（品質確認・任意）
- [ ] 存在しないifnameを含むconfigで実行 → 当該ポートのみ NG、他はOK（隔離）
- [ ] 不正IPを含む行 → 当該行のみ NG・ログ記録、処理継続
- [ ] ケーブル未接続ポート → 設定は入るが状態に応じ判定（運用で確認）

---

## Phase 2 — AMD実機（量産機）

- [x] Intel検証機のSSDをAMD実機へ移設
- [x] `python3 tools/inventory.py` を **AMD実機で再実行**
      → インターフェース名を確認（Intel機の `enp4s0` はAMD機に存在せず）
- [x] 結果を `config/config_amd.ini` に反映（このファイル追加が移行作業の本体）
- [x] Intel I350-T4V2 全6ポートの認識確認: `nmcli device status`
- [x] `--commit` で一連動作を再確認（LAN2〜6で `PASS (OK=5 NG=0)`）
- [ ] **LAN1（`enp3s0`）の本適用確認** … リモート接続保持のため未実施。
      ローカル環境で要確認
- [ ] **LAN1/LAN2 の物理ラベル対応確認** … Realtek(`r8169`)は `ethtool -p` 非対応のため
      ケーブル抜き差しで確認（`/sys/class/net/<if>/carrier`）
- [ ] NIC認識順序・M/B固有差異の有無を確認
      （軽微差異=見積内対応 / 設計変更要=別途見積 … 見積書 備考参照）

### D. ボード設定の自動判別（Phase 2 追加機能）

| # | 項目 | コマンド / 操作 | 合格条件 |
|---|------|----------------|----------|
| 13 | CLI自動判別 | `python3 -m ipset.cli test_safe.csv --serial TEST001`（`--config` 無し） | `Board config: config/config_amd.ini (AMD-QC: 6/6 ports matched)` と表示 |
| 14 | 判別ロジック | 下記「判定確認コマンド」 | AMD=6/6、Intel=5/6（`enp4s0` 未検出）と表示 |
| 15 | 不一致時の安全動作 | `config_amd.ini` を一時退避して実行 | **終了コード2** で停止。Intel設定へ勝手に切替えない |
| 16 | GUI自動判別 | GUIを起動 | 起動時に「設定INI」欄が `config/config_amd.ini` になる |
| 17 | 導入版での自動判別 | アイコンから起動 | 同上（`/opt/ipset/config/` に両INIが存在すること） |

判定確認コマンド:
```
python3 -c "
from ipset.core import detect
for m in detect.evaluate_configs('config'):
    print('%-28s %d/%d  missing=%s' % (m.path, m.matched, m.total, m.missing or '-'))
"
```

> **#17 の注意**: アイコン起動は `/opt/ipset` の導入版を実行します。ソース側の変更は
> `bash build.sh` → `sudo bash setup_desktop.sh` を実行するまで反映されません。

---

## 検収時の提出物（城迫様指定）
- [ ] 納品書
- [ ] 請求書
- [ ] 納品物: Pythonソース一式 / 実行プログラム(dist) / config / 操作マニュアル
